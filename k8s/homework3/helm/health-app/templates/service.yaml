apiVersion: v1
kind: Service
metadata:
  name: {{ include "health-app.fullname" . }}
  labels:
    {{- include "health-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "health-app.selectorLabels" . | nindent 4 }}
